# Language Extension — Open Questions

## Map key constraints

Restrict to String + Number (simple hashing) or arbitrary values (needs `hash()` protocol on
instances)? String + Number covers 95% of use cases.
