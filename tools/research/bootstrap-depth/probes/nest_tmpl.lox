fun nest(n) { var v = []; var i = 0; while (i < n) { v = [v]; i = i + 1; } return v; }
fun deep(d, v) { if (d == 0) { print v; return 0; } return deep(d - 1, v); }
deep(__D__, nest(__N__));
print "done";
