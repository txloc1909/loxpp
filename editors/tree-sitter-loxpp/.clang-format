# The C and C++ sources under this directory are generated by
# `tree-sitter generate` or come from the tree-sitter project scaffold.
# Keep clang-format away from them so a regeneration stays a clean diff.
DisableFormat: true
SortIncludes: false
