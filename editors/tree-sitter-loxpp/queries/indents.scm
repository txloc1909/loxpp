[
  (block)
  (block_arm_body)
  (class_body)
  (match_expression)
  (map)
  (list)
  (arguments)
  (parameters)
] @indent.begin

[
  "}"
  "]"
  ")"
] @indent.branch

[
  "}"
  "]"
  ")"
] @indent.end

(comment) @indent.auto
